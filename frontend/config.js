// Point this at wherever the PyChronicle backend (FastAPI/uvicorn) is running.
// Leave as an empty string '' if frontend and backend are served from the
// same origin (e.g. backend also serves these static files).
const API_BASE_URL = "http://localhost:8000";
